# Copy to .env (gitignored) and fill in. The CLI auto-loads .env from cwd.
# Tokens stored in your OS keyring (`hydra configure`) take precedence over these.

HYDRA_SELF_HOSTED_GITLAB_TOKEN=glpat-...
HYDRA_GITLAB_TOKEN=glpat-...
HYDRA_GITHUB_TOKEN=ghp_...
